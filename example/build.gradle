version 'unspecified'

apply plugin: 'java'

sourceCompatibility = 1.7

repositories {
    mavenCentral()
}

dependencies {
    compile project(":lib")
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
